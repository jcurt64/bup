import PrototypeFrame from "../_components/PrototypeFrame";

export const metadata = {
  title: "BUPP — Espace Pro",
};

export default function ProPage() {
  return <PrototypeFrame route="pro" />;
}
